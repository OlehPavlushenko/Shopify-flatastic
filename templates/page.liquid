<!-- /templates/page.liquid -->
<div class="grid">
  <div class="grid__item large--one-whole">
      <h1>{{ page.title }}</h1>
      <div class="rte--nomargin rte--indented-images">
         {{ page.content }}
      </div>
  </div>
</div>
